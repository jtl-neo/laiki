這套系統的最終型態，是一套「基於一對一對話、支援漸進式虛擬綁定（Lazy Loading）、且整合雙式記帳維度」的 AI 記帳系統。
以下為你整理最完整的系統架構與實際運作範例，你可以直接作為專案規格書（Spec）或開發藍圖使用。
🏛 1. 系統完整架構 (System Specification)
🧩 A. 四大核心技術層級
前端/交互層 (Line Frontend)：
採用 1對1 獨立私密對話（不進群組）。
利用 Bento Design (便當塊風格) Flex Message 呈現模組化圖表與對帳單。
提供 Quick Reply 快速按鈕作為 AI 追問時的一鍵回覆。
AI 語意層 (LLM Gateway)：
動態注入目前該使用者已建立的「好友與影子帳號別名清單」作為 Context。
強制 LLM 嚴格輸出預設的結構化 JSON Schema。
狀態機與路由層 (State Machine & Router)：
Context Cache：使用 Redis 暫存 is_complete: false 的不完整交易。
Transaction Router：根據交易類型將資料分流至：個人帳 (expense)、分帳 (split_expense)、基金帳 (fund_expense)。
資料結構層 (Database - PostgreSQL)：
支援 is_virtual 欄位，允許「先記帳、事後綁定」的影子帳號機制。
🗄 B. 核心資料庫關聯表 (Database Schema)
1. 使用者表 (users)
記錄實體使用者與虛擬影子帳號。
id (UUID, PK)
line_user_id (String, Unique, Nullable) -> 影子帳號時為 NULL，綁定後填入
display_name (String) -> 記帳人幫他取的辨識暱稱（如 "a"、"老王"）
is_virtual (Boolean) -> 是否為影子帳號（預設為 true）
created_by (UUID, Nullable) -> 記錄這個影子帳號是「誰」建立的
2. 支付手段帳戶表 (payment_methods)
id (UUID, PK)
user_id (UUID, FK) -> 屬於哪位實體使用者
name (String) -> 帳戶名稱（如 "富邦卡"、"Line Pay"、"現金"）
type (Enum) -> CASH (立刻扣款), CREDIT (累計帳單), FUND (共同基金)
3. 分帳債權表 (debts)
id (UUID, PK)
transaction_id (UUID, FK)
creditor_id (UUID, FK) -> 債權人（誰先墊錢，通常是 me）
debtor_id (UUID, FK) -> 債務人（可指向虛擬影子帳號的 UUID）
amount (Numeric) -> 欠款金額
status (Enum) -> PENDING (未還), SETTLED (已還清)
📝 2. LLM 統一結構化 JSON Schema
不論何種記帳情境，後端 API 接收到的 AI 解析結果皆符合以下格式：



JSON
{
  "type": "expense" | "split_expense" | "fund_expense",
  "is_complete": true | false,
  "missing_fields": ["total_amount", "payment_method", "debt_distribution"],
  "data": {
    "description": "消費品項與場景描述",
    "total_amount": 500,
    "payment_method": "富邦卡", 
    "payer_id": "me" | "common_fund",
    "my_share": 120,
    "debts": [
      { "name": "a", "amount": 180 },
      { "name": "b", "amount": 200 }
    ],
    "fund_name": "家裡用的東西"
  }
}


🎬 3. 全場景運作範例 (End-to-End Scenarios)
💡 範例一：複雜多人分帳（觸發影子帳號與預設支付）
1. 使用者輸入（你對 Bot 說）：
「今天午餐和 a 和 b 吃麥當勞，我先出錢，a 吃 180，b 吃 200，總共花了 500」
2. 後端運作流程：
Context 檢查：後端撈取你的好友清單，發現目前沒有叫 a 和 b 的好友。
LLM 解析：LLM 自動做數學題（500 - 180 - 200 = 120），判斷 my_share 為 120，但發現使用者沒有說用什麼付錢。
AI 回傳 JSON：
JSON
{ "type": "split_expense", "is_complete": false, "missing_fields": ["payment_method"], "data": { ... } }





*   **狀態機追問**：後端將資料暫存至 Redis，並在 Line 彈出 Quick Reply 按鈕。

#### 3. Line 介面互動：
*   **Bot 追問**：
    「已為您準備好麥當勞分帳明細！請問您剛才是用什麼方式付款的呢？」
    *(下方彈出按鈕：`[ 💳 富邦卡 ]` `[ 🟢 Line Pay ]` `[ 💵 現金 ]`)*
*   **你點擊**：`[ 💳 富邦卡 ]`
*   **資料庫寫入**：
    1. 在 `users` 表自動生成兩個影子帳號：`a` (Virtual), `b` (Virtual)。
    2. 你的富邦卡帳戶新增一筆 `+$500` 未結帳單。
    3. 建立兩筆債權：`影子帳號 a 欠你 180`、`影子帳號 b 欠你 200`。
*   **Bot 最終回覆（Bento Card）**：
    ```
+-----------------------------------------+
| 🍔 麥當勞午餐 (分帳成功)                 |
+--------------------+--------------------+
| 💳 支付：富邦卡      | 💰 總額：$500       |
+--------------------+--------------------+
| 👤 你的分攤：$120                        |
| 👤 a (未綁定) 應付：$180                 |
| 👤 b (未綁定) 應付：$200                 |
+-----------------------------------------+


🔗 範例二：事後綁定（認領歷史帳目）
過了一個禮拜，你跟 a 碰面，a 想要看他欠你多少錢並加入系統。
1. 互動流程：
你操作：在你的 Rich Menu 點擊「管理好友」-> 點選「a (未綁定)」-> 點擊「產生綁定碼」。
Bot 回覆：給予一組 PIN 碼 882391。
a 操作：a 用自己的手機加這個 Line Bot 為好友，並在對話框直接輸入：882391。
2. 後端 Merge 邏輯：
後端收到 882391，偵測到觸發綁定：



SQL
UPDATE users 
SET line_user_id = 'A_REAL_LINE_ID', is_virtual = false 
WHERE id = '影子帳號_a_的_UUID';


3. a 的 Line 畫面立刻覺醒：
a 的對話框秒速跳出整合好的 Bento 歷史帳單卡片：



+-----------------------------------------+
| 🎉 歡迎加入！已成功認領歷史帳目          |
+-----------------------------------------+
| 您目前與 Lin Jing-tang 的未結帳務：       |
|                                         |
| 🔴 您總共欠對方：$180                    |
|                                         |
| [明細] 2026/06/05 麥當勞午餐  +$180     |
+-----------------------------------------+
|         [ 點擊一鍵發起 LINE PAY 還款 ]   |
+-----------------------------------------+


🏡 範例三：共同基金扣款（三人皆已綁定，或部分綁定）
你、c、d 已經共同綁定了名為「家裡用的東西」的虛擬基金帳戶（當前餘額：$5,000）。
1. 使用者輸入（你對 Bot 說）：
「用共同基金買了家裡用的衛生紙和洗衣精 450 元」
2. 後端與 AI 運作：
LLM 解析：偵測到關鍵字「共同基金」，匹配到對應的基金項目。
後端扣款：
檢查 shared_funds 中「家裡用的東西」餘額（$5,000 > $450，充足）。
直接在資料庫將該基金餘額減去 $450（新餘額：$4,550）。
記錄一筆由 common_fund 支付的流水平帳。
3. 跨帳號聯動主動通知 (Push Message)：
此時，你、c、d 三個人的 1對1 視窗 會同時收到這筆基金變動通知（不論他們是不是影子帳號，只要已綁定的實體帳號都會收到）：



+-----------------------------------------+
| 🏡 共同基金變動變更                     |
+-----------------------------------------+
| 帳本：家裡用的東西                       |
| 品項：衛生紙和洗衣精                     |
| 金額：-$450 (由 Lin Jing-tang 登記)      |
+-----------------------------------------+
| 💰 基金當前剩餘額度：$4,550              |
+-----------------------------------------+


🚀 總結：這套架構厲害在哪裡？
記帳零阻力：你想記就記，朋友不當場加好友也完全沒關係，系統會用「影子帳號」幫你妥善保管債權。
認領無縫隙：朋友事後輸入 PIN 碼，過去的所有爛帳、基金變動一秒同步，體驗極佳。
架構極度嚴謹：雖然前端是一句口語，但後端已經把「誰付的錢（資產端）」和「這筆錢歸誰（負債/費用端）」的雙式記帳邏輯處理得清清楚楚。
